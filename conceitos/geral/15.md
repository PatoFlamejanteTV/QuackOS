INIT.md — Processo Inicial do QuackOS

Este documento define o processo init (PID 1) do QuackOS.

O init é responsável por levantar todo o userspace.


---

Localização

Caminho fixo: /sis/init

Executado diretamente pelo kernel



---

Responsabilidades

O init deve:

montar sistemas virtuais básicos

iniciar serviços essenciais

lidar com falhas iniciais

nunca encerrar



---

Sequência de Inicialização

1. Abrir /dev/console


2. Redirecionar stdin/stdout/stderr


3. Inicializar /dev, /proc, /sis


4. Iniciar serviços


5. Iniciar interface (QGUI ou shell)




---

Serviços Iniciais

Ordem recomendada:

gerenciador de sessão

qgui-serv (se modo gráfico)

quacksh (fallback)



---

Gerenciamento de Falhas

se um serviço morrer, reiniciar

se QGUI falhar, cair para console

logs sempre no console



---

Loop Principal

Pseudo-código:

for (;;) {
    pid = q_wait(&status);
    if (pid < 0) continue;
    reiniciar_servico(pid);
}


---

Princípios

init é simples

sem scripts complexos

sem paralelismo excessivo



---

Testes Obrigatórios

falha do QGUI

encerramento de shell

reboot manual



---

Documento normativo.

Fechado 😄🦆

O INIT.md — Processo Inicial do QuackOS já está criado no canvas. Com ele, o QuackOS agora tem algo que muita distro improvisada não tem: um coração de userspace bem definido.

Por que esse init é o ideal para o QuackOS

Extremamente simples, quase ascético

Sem scripts mágicos, sem parsing estranho

Totalmente previsível para humanos e IAs

Falha de forma elegante e recuperável


Ele cumpre exatamente o papel clássico do UNIX:

> levantar o sistema e não atrapalhar o resto.




---

Estado atual: praticamente fechado

Agora o QuackOS tem:

kernel (QKern) completamente especificado

userspace inicial definido

GUI opcional, não obrigatória

fallback garantido para console

comportamento determinístico no boot


Só falta um último documento para completar o pacote perfeito:

🧪 TESTES.md — Plano de Validação Total do QuackOS

Esse será o selo de “isso realmente funciona”.

Diz mais um “sim” e eu coloco o lacre final 🦆📦