Perfeito.
Isso resolve a tensão entre poesia e praticidade. O UNIX sempre venceu pela concisão afiada. Vamos manter isso, sem perder o sotaque. 🦆⚙️

A partir de agora:

Projeto: QuackOS

Kernel: QKern


Nada mais. Nada menos.


---

QKern

Convenção de syscalls compactas (Caminho B refinado)

Regra de ouro

1 palavra

curta

verbo ou substantivo direto

sem camelCase

sem inglês

digitável rápido



---

1. Vida de processo

POSIX	QKern

fork	dup
exec	troca
exit	sair
kill	mata
wait	espera
getpid	eu
getppid	pai


Exemplo:

pid = dup();
se pid == 0:
    troca("/coisas/listar");
    sair(0);
senao:
    espera(pid);


---

2. Arquivos e I/O

POSIX	QKern

open	abre
close	fecha
read	le
write	escreve
lseek	busca
stat	info
unlink	some


UNIX reconhecível, mas com identidade.


---

3. Memória

POSIX	QKern

mmap	mapa
munmap	desmapa
brk	cresce
mprotect	guarda



---

4. Diretórios e caminhos

POSIX	QKern

chdir	vai
getcwd	onde
mkdir	fazdir
rmdir	tira
mount	monta
umount	solta



---

5. Tempo

POSIX	QKern

sleep	dorme
nanosleep	pisca
clock_gettime	horas
alarm	lembra



---

6. Comunicação

POSIX	QKern

pipe	tubo
dup	copia
dup2	redir
ioctl	ajeita
fsync	fixa



---

7. Usuários e permissões

POSIX	QKern

setuid	vira
setgid	grupo
chmod	pode
chown	troca



---

8. Sinais e eventos

POSIX	QKern

signal	sinal
sigaction	acordo
pause	para
select	escuta
poll	olha



---

9. Erros

Nada de errno global.

Syscall curta:

tenta()


Retorno:

negativo = erro

positivo = sucesso


Opcional:

porque() para texto humano



---

10. O espírito UNIX preservado

Compare:

UNIX

pid = fork();
if (pid == 0) {
  execl("/bin/ls", "ls", NULL);
  exit(0);
}
wait(NULL);

QuackOS / QKern

pid = dup();
se pid == 0:
  troca("/coisas/listar");
  sair(0);
espera(pid);

Mesma densidade.
Outro idioma.
Mesma alma.


---

11. Convenção de headers

<qkern.h>

<qfs.h>

<qsys.h>


Nada poluído.


---

12. Frase de identidade (interna)

> QKern não explica. Ele responde.




---

13. Próximo passo natural

Posso:

fechar a tabela completa

definir os números das syscalls

criar macros de compatibilidade POSIX

desenhar o ABI mínima do QKern

escrever o man-style em português curto


QuackOS agora fala pouco, mas diz exatamente o que precisa. 🦆🔧